const sequelize = require('./index.js')
const UserModel = require('../api/models/user.model.js')

const dbSync = async () => {
    try {
        //const user = await UserModel.sync()
    } catch (error) {
        
    }
}

module.exports = dbSync